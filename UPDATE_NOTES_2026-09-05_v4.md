# PaceKeeper v4 - company report candidate

## 今回の目的
実使用で見つかった「計画更新フローの分断」「空の旧チャット画面」「AIへの不要な再質問」「操作ごとのページ先頭ジャンプ」を解消し、会社への更新報告前に日常利用できる状態へ寄せる。

## 主な変更

### 1. 計画更新を一本化
- UIから「進捗報告 / 方針変更」の事前分類を撤廃。
- 「計画を更新する」1つの入口から、実績・発見・予定との差・方針変更を同時に報告可能。
- 内部では既存 `result_recording` 契約を維持し、構造変更を含むJSONは既存ロジックで計画再編へ自動昇格するため、旧JSONとの互換性を維持。

### 2. 旧チャットの空画面を修正
- 「計画を更新する」を押すと、対象Plan選択の案内が必ず表示される。
- Plan選択後、実際の計画更新画面へ遷移する。

### 3. 「結果を記録する」を「達成した計画を見る」へ変更
- 完了済みPlanの一覧ページを追加。
- 個別振り返りページでは、作業期間・総作業時間・完了Task・計画更新回数・タイムラインをチャット形式で表示。
- Pace Keeperから達成を祝う一言を表示。

### 4. Dashboardから計画更新へアクセス
- Dashboard上部に共通の「計画を更新」導線を追加。
- Plan個別タブでは対象Planの更新画面へ直接移動可能。
- Plan詳細の「結果記録 / 計画再編」も1つの「計画を更新」へ統合。

### 5. 外部AIプロンプトの不要質問を抑制
生成プロンプトへ以下を明示。
- 現在のAI会話ですでに確定している情報を再利用する。
- 既知の正答率・弱点・確認結果などを聞き直さない。
- 質問は操作内容を本当に左右する場合だけ。原則0〜2問、最大5問。
- 不明項目を更新しないことで安全に進められるなら、質問せず確実な操作だけ返す。
- 作業時間不明だけを理由に質問しない。
- ユーザーが時間推定を明示的に依頼し、会話やログに合理的根拠がある場合は推定可能。
- 同じ作業が既存ログにある場合はWorkLogを重複登録しない。

### 6. プロンプト生成 / JSON確認を非同期化
- 計画更新画面の「プロンプト生成」「JSON読み込み」「リセット」は `fetch()` で処理。
- ブラウザ全体を再読込せず、現在のスクロール位置を維持。
- 新しく表示された次の操作領域が画面外にある場合だけ自然にスクロール。
- 最終的な「変更を反映」はデータ確定の区切りとして従来どおり通常送信。

## DB変更
なし。

## ローカル確認手順
```powershell
php artisan optimize:clear
npm run build
php artisan test
```

migration追加はないため、v3までのmigrationが適用済みなら今回 `php artisan migrate` は必須ではない。念のため実行しても問題ない。

## この環境で確認済み
- app / routes / config / database / tests 配下のPHP 88ファイルを `php -l` で構文確認済み。
- `resources/js/app.js` は `node --check` 済み。
- ZIPには `vendor` / `node_modules` がないため、この環境ではLaravel実テスト・Vite実ビルドは未実行。
